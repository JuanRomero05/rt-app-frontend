import axios from "axios"
import { environment } from "src/environments/environment"

const url = environment.api

export const getRequest = async (route: string, headers: any) => {
}

export const postRequest = async (route: string, headers: any, body: any) => {
}

export const putRequest = async (route: string, headers: any, body: any) => {
}

export const deleteRequest = async (route: string, headers: any) => {
}